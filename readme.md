# An Implimentation of LZ78 + bitpacking in Java.#

Can be run using the scrips lz78c.sh and lz78d.sh to encode and decode respectively.

Encoding a file:

>./lz78c.sh [filename] [maxPhraseSize]

Decoding a file

>./lz78d.sh [filename]
